<?xml version="1.0" encoding="UTF-8"?>
<table xmlns="http://query.yahooapis.com/v1/schema/table.xsd">
  <meta>
    <sampleQuery>select * from {table} where uids='cat';</sampleQuery>
  </meta>
  <bindings>
    <select itemPath="" produces="XML">
      <urls>
        <url>http://amos.im.alisoft.com/muliuserstatus.aw?uids={uids}</url>
      </urls>
      <inputs>
        <key id='uids' type='xs:string' paramType='variable' required="true" />
      </inputs>
      <execute><![CDATA[
          // Your javascript goes here. We will run it on our servers
		  rawStatus = request.get().response;
		  var url = request.url + uids;
		  var myRequest = y.rest(url);
	      var data = myRequest.get().response;
          response.object = <info>
								<url>{url}</url>
								<uids>{uids}</uids>
								<data>{data}</data>
							</info>;
      ]]></execute>
    </select>
  </bindings>
</table>